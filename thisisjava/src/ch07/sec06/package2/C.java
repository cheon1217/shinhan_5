package ch07.sec06.package2;

//import ch07.sec06.package1.A;
// A 클래스와 다른 패키지에 있기 때문에 protected에 접근할 수 없다.

public class C {
	public void method() {
		// 전부 불가능
//		A a = new A();
//		a.field = "value";
//		a.method();
	}
}
